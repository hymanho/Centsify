module.exports = {
  purge: [],
  darkMode: false, // or 'media' or 'class'
  theme: {
    extend: {
      animation: {
        'spin-slow': 'spin 3s linear infinite', // 3s is slower than the default 1s
      },
    },
  },
  variants: {},
  variants: {
    extend: {},
  },
  plugins: [],
};

